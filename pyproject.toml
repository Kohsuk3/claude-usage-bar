[project]
name = "claude-usage-bar"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "rumps>=0.4.0",
]

[dependency-groups]
dev = [
    "py2app>=0.28.10",
]
